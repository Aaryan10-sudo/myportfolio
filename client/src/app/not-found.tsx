import { NotFoundAnimation } from "@/animation/not-foundAnimation";
import React from "react";

const page = () => {
  return (
    <div className="min-h-screen">
      <NotFoundAnimation />
    </div>
  );
};

export default page;
